import 'package:flutter/cupertino.dart';

class FailedSettings extends StatelessWidget {
  const FailedSettings({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
        child: Text('Sätete laadimine ebaõnnestus')
    );
  }
}
