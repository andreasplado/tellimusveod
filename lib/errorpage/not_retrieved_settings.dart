import 'package:flutter/cupertino.dart';

class NotRetrievedSettings extends StatelessWidget {
  const NotRetrievedSettings({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Ei leitud seadmeid')
    );
  }
}
